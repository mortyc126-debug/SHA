# BTE RESEARCH PROGRAM — Теоретические вопросы

## Что построено

### Объекты
1. **BTE** — Bi-Temporal Element. Математический объект с двумя временами (round r, bit k).
2. **★-Algebra** — раундовая функция как неделимый оператор. 93.7% fixed + 6.3% variable.
3. **Bit-Layer Decomposition** — SHA-256 = 4 квадратичных слоя по 127 + хво��т 4.
4. **BTE Class** — класс всех BTE с 8-register shift + створочное coupling.

### Теоремы
1. **Layer Rank = 2R-1** — универсал��но для класса BTE. Док��зано.
2. **Quadratic Deficit ≈ 13.5%** — Ch/Maj отс��кают 13.5% trajectories. Layer 0 only.

### Ключевые числа SHA-256
- 512 = 4 × 127 + 4
- 127 = 2×64 - 1 (створочное = 1 dependency)
- 93.7% Яко��иана фиксировано
- 6.3% = 4149 variable entries (carry)
- Три взаимодействия: NL+local, L+global, seq+bridge
- Дефицит = ~27 бит (только Layer 0)
- Все межслойные корреляции = 0 (MI verified)

---

## ОТКРЫТЫЕ ТЕОРЕТИЧЕСКИЕ ВОПРОСЫ

### Уровень 1: Теория BTE класса

**P1.1: Аналитическое доказательство створочного числа на bit-0 уровне**
Мы доказали 2R-1 через измерение (dep=1 для вс��х R). Нужно АНАЛИТИЧЕСКОЕ доказательство:
почем�� створочное число создаёт ровно 1 GF(2) зависимость на bit-0.
Конкретно: вывести формулу зависимости e[r][0] от a[r..r-4][0] и ��оказать rank loss = 1.

**P1.2: Формула числа чистых слоёв**
pure_layers = f(n, rotations). Изме��ено: n=8→3, n=16→8, n=32→4.
Зависит от ротаций и n. Нет формулы. Нужна теорема связывающая
pure_layers с coverage speed ротаций.

**P1.3: Классификация BTE ��о стойкости**
Как��е параметры (n, σ, R) определяют "стойкость" BTE?
Слабые BTE: gcd(rotations, n) > 1 → неполное покрытие.
Сильные BTE: SHA-256. Что МЕЖДУ ними? Характеризова��ь.

**P1.4: Квадратичный дефицит — точная формула**
deficit ≈ 13.5% при n=4,5. Масштабируется ли к n=32?
Зависит ли от конкретных Ch/Maj или от ЛЮБОЙ degree-2 функции?
Если заменить Ch → другую degree-2 функцию — дефицит тот же?

### Уровень 2: Связь BTE с вычислительной сложностью

**P2.1: BTE Collision Complexity Theorem**
Доказать или опровергнуть: для BTE с параметрами (n, R, σ):
  collision_cost ≥ 2^{n·n_regs/2} (birthday bound)
с точностью до полиномиального фактора.
Если не удаётся доказать → найти BTE ��де collision < birthday.

**P2.2: XL Complexity через BTE структуру**
Наши XL оценки (2^38 per layer) были для trajectory, не hash.
Пересчитать для HASH-based системы (256 equations, 512 unknowns для SHA-256).
Сравнить с OMEGA's XL estimate.

**P2.3: Layered vs Monolithic solving**
Теоретически сравнить:
  A) Solve 512 degree-2 equations сразу (OMEGA approach)
  B) Solve 4 × 127 degree-2 equations послойно (BTE approach)
  Для случайных degree-2 систем: A и B эквивалентны?
  Для BTE-structured систем: B может быть дешевл��?

### Уровень 3: Новая математика (выход за BTE)

**P3.1: Нелинейные инварианты BTE**
Линейные инварианты = локальные (state-dependent). Не помогают.
Квадратичные инварианты f(state) = const, где f = degree 2?
Или f(state[r], state[r+k]) = const для нелинейного f?

**P3.2: BTE "спектральная теория"**
Для линейных систем есть спектр (eigenvalues). Для BTE: что играет роль
спектра? Mixing time зависит от ротаций — это "спектральное" свойство.
Формализовать: определить "BTE spectrum" и связать с mixing/security.

**P3.3: Carry Operator Algebra**
Carry = MAJ sequential recursion. Определить carry как ОПЕРАТОР на
пространстве bit-vectors. Свойства: nilpotent? idempotent? Спектр?
Carry оператор action на BTE state space ��� алгебра этого action.

**P3.4: "Грамматика" как математический объект**
Schedule = еди��ственная грамматика SHA-256. Формализовать:
  - Schedule как линейный оператор L: GF(2)^{512} → GF(2)^{1536}
  - Ядро schedule = 0 (full rank, доказано)
  - Образ schedule = 512-dim подпространство в W-space
  - Как образ schedule ВЗАИМОДЕЙСТВУЕТ с BTE layer structure?

**P3.5: Cross-hash BTE analysis**
SHA-256, SHA-512, BLAKE2, Keccak — все = конкретные BTE (или не BTE?).
Какие hash functions принадлежат классу BTE? Какие — нет?
Keccak = sponge, не Merkle-Damgård → не BTE. Но имеет ли аналог?
Обобщить BTE до клас��а, покрывающего ВСЕ хеш-функции.

### Уровень 4: Долгосрочные направления

**P4.1: BTE как основа для доказательства стойкости**
Можно ли ДОКАЗАТЬ что SHA-256 collision = 2^128 испо��ьзуя BTE?
Если BTE Class Theorem + дополнительные условия → lower bound?
Это было бы ОГРОМНЫМ результатом для криптографии.

**P4.2: Конструктивное BTE — новые хеш-функции**
Используя BTE теорию: сконструировать хеш-функцию с ДОКАЗУЕМОЙ стойкостью
(или заданной стойкостью). BTE параметры → security level.

**P4.3: Квантовая BTE**
Grover's algorithm на BTE: 2^{64} для SHA-256. Наша слоистая структура
(4 layers) — может ли она дать квантовое ускорение через Grover per layer?

---

## ПРИОРИТЕТЫ

**Ближайшие (след��ющая сессия):**
- P1.1 (аналитическое доказательство)
- P1.4 (дефицит масштабирование)
- P2.3 (layered vs monolithic)

**Среднесрочные (3-5 сессий):**
- P1.2, P1.3 (классификация BTE)
- P3.1, P3.2 (новая математика внутри BTE)
- P3.4 (грамматика формально)

**Долгосрочные (10+ сессий):**
- P2.1 (complexity theorem)
- P3.5 (cross-hash)
- P4.1, P4.2 (applications)

---

## ФОРМАТ РАБОТЫ

1. Каждый вопрос P*.* = одна задача. Результат: теорема или закрытие.
2. Каждая теорема: формулировка + доказательство (аналитическое или экспериментальное).
3. Каждое закрытие: почему н�� работает, что нового узнали.
4. Не искать аналоги. Создавать.
5. Не ходить по кругу. Читать SESSION_RESULTS перед каждой сессией.
